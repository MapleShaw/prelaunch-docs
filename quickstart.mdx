---
title: 'Quick Start'
description: 'Get up and running with PreLaunch in minutes'
---

## Installation

Follow these steps to get started with PreLaunch:

### 1. Clone the Repository

```bash
git clone https://github.com/your-repo/prelaunch.git
cd prelaunch
```

### 2. Install Dependencies

Use your preferred package manager to install the dependencies:

```bash
npm install
# or
yarn install
# or
pnpm install
```

### 3. Configure Environment Variables

Copy the `.env.local.example` file to `.env.local` and fill in the necessary environment variables:

```bash
cp .env.local.example .env.local
```

Key environment variables:

```
# Basic Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret

# Google Analytics (optional)
NEXT_PUBLIC_GA_MEASUREMENT_ID=your_ga_id

# Typeform (optional)
TYPEFORM_ACCESS_TOKEN=your_typeform_access_token
TYPEFORM_VOTE_FORM_ID=your_vote_form_id
TYPEFORM_SUGGESTION_FORM_ID=your_suggestion_form_id

# Other Services (optional)
# MONGODB_URI=your_mongodb_uri
# RESEND_API_KEY=your_resend_api_key

# Paddle Configuration
PADDLE_SANDBOX=true
PADDLE_API_KEY=your-api-key-here
PADDLE_NOTIFICATION_WEBHOOK_SECRET=your-webhook-secret-here
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=your-paddle-client-token-here
```

### 4. Start the Development Server

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```

Visit [http://localhost:3000](http://localhost:3000) to view your application.

## Basic Configuration

### Project Configuration

Edit the `config.ts` file to change the application name, description, domain, and other basic information:

```typescript
const config = {
  // REQUIRED
  appName: "YourAppName",
  // REQUIRED: Short description of your app for SEO tags
  appDescription: "Your app description here",
  // REQUIRED (no https://, no trailing slash, just the naked domain)
  domainName: "yourdomain.com",
  // Other configuration...
}
```

### Customize Content

Edit the translation files in the `locales/` directory to change the website text content:

- `locales/en/` - English text
- `locales/zh/` - Chinese text

### Style Customization

- Edit `tailwind.config.js` to customize colors, fonts, etc.
- Edit `styles/globals.css` to add custom CSS

## Deploying Your Project

### Build for Production

```bash
npm run build
# or
yarn build
# or
pnpm build
```

### Deploy to Vercel

PreLaunch can be deployed to Vercel with one click:

<Card
  title="Deploy to Vercel"
  icon="rocket"
  href="https://vercel.com/new/git/external?repository-url=https://github.com/your-repo/prelaunch"
>
  Click to deploy to Vercel
</Card>

### Deploy to Netlify

You can also easily deploy to Netlify:

<Card
  title="Deploy to Netlify"
  icon="rocket"
  href="https://app.netlify.com/start/deploy?repository=https://github.com/your-repo/prelaunch"
>
  Click to deploy to Netlify
</Card>

## Next Steps

- [Configuration](/customization/configuration) - Learn how to configure your application
- [Multi-language Support](/features/multilanguage) - How to add more languages
- [Payment Integration](/features/payments) - Set up Paddle/Stripe payments
